using UnityEngine;
using System.Collections;
using UnityEngine.UI;

// holds the refs to all the Text, Images on the card
public class OneCardManager : MonoBehaviour {
/*
    public CardAsset cardAsset;
    public OneCardManager PreviewManager;
    
	[Header("Text Component References")]
    public Text CardDescriptionText;
    public Text CardNameText;
    public Text CardPower;
    public Text CardAdditionalPower;


    [Header("Image References")]
    public Image CardFaceImage;


    void Awake()
    {
        if (cardAsset != null)
            ReadCardFromAsset();
    }

    public void ReadCardFromAsset()
    {

		CardDescriptionText.text = cardAsset.Description;

		CardNameText.text = cardAsset.characterName;

		CardPower.text = cardAsset.CharacterPower;

		CardAdditionalPower.text = cardAsset.AdditionalPower;

		CardFaceImage.sprite = cardAsset.CardImage;




        if (PreviewManager != null)
        {
            // this is a card and not a preview
            // Preview GameObject will have OneCardManager as well, but PreviewManager should be null there
            PreviewManager.cardAsset = cardAsset;
            PreviewManager.ReadCardFromAsset();
        }
    }
    */
}
